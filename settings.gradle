pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // 下面三行
        maven {
            url 'https://artifact.bytedance.com/repository/pangle'
        }
    }
}

rootProject.name = "遇见广告"
include ':app'
